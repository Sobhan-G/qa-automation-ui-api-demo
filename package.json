{
  "name": "qa-automation-ui-api-demo",
  "version": "1.0.0",
  "description": "QA automation demo combining UI and API testing using Playwright",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
